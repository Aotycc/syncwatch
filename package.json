{
  "name": "syncwatch",
  "version": "1.0.0",
  "description": "实时视频同步观影 - 部署在 Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
